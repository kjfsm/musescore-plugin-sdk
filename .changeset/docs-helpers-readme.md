---
"@kjfsm/musescore-plugin-sdk-helpers": patch
---

Update README to document all exported modules: `annotations`, `tracks`, `note-type`, `element-types`, and functions added since the initial release (`iterateMeasureSegments`, `iterateStaves`, `getNoteTypeName`, `isGraceNote*`, element-type guards and accessors, etc.).
